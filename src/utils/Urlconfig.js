const baseUrl = "http://localhost:3000";

const API_URL = {
    LOGIN: baseUrl + "/users/login",
    REGISTER: baseUrl + "/users/register",
    UPDATE_PROFILE: baseUrl + "/users/update-profile",
    UPDATE_PASSWORD: baseUrl + "/users/update-password",
}

export {API_URL}